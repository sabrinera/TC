spring.datasource.url = jdbc:mysql://localhost:3306/gestioncertificat
spring.datasource.username = root
spring.datasource.password =

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto = update

# Naming strategy
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager)

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect
#spring.mvc.view.prefix: /WEB-INF/jsp/
#spring.mvc.view.suffix: .jsp

# spring.jackson.date-format=yyyy-mm-dd

#mail properties
spring.mail.host = smtp.gmail.com
spring.mail.username = sabrinerahhou@gmail.com
spring.mail.password = Dontworrybehappy17

Spring.mail.propreties.mail.smtp.auth = true 
Spring.mail.propreties.mail.smtp.socketFactory.port =  465
Spring.mail.propreties.mail.smtp.socketFactory.class = javax.net.SSLSocketFactory
Spring.mail.propreties.mail.smtp.socketFactory.fallback = false